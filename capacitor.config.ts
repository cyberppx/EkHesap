import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efkwnlabs.ekhesap',
  appName: 'EkHesap',
  webDir: 'www', // Burası sadece 'www' olmalı
  bundledWebRuntime: false
};

export default config;